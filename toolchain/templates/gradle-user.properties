# Copy to %USERPROFILE%\.gradle\gradle.properties (managed by patch-flutter-gradle-mirrors.ps1)
# Gradle distribution: use Tencent mirror in each project's gradle-wrapper.properties
#
# SOCKS lines are added ONLY when 127.0.0.1:10808 is reachable (smart detection).

systemProp.java.net.useSystemProxies=false
